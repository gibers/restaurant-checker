
            // Récupérer tous les attributs de l'élément via JavaScript
//        JavascriptExecutor jsExecutor = (JavascriptExecutor) driver;
//        Map<String, Object> attributes = (Map<String, Object>) jsExecutor.executeScript(
//                "var items = {}; " +
//                "for (var i = 0; i < arguments[0].attributes.length; i++) { " +
//                "  items[arguments[0].attributes[i].name] = arguments[0].attributes[i].value; " +
//                "} " +
//                "return items;",
//                listResultatRestaurant
//        );

        // Afficher les attributs et leurs valeurs
//        System.out.println("Attributs de l'élément :");
//        for (Map.Entry<String, Object> entry : attributes.entrySet()) {
//            System.out.println(entry.getKey() + " = " + entry.getValue());
//        }

